import React from "react";
import HeaderWidget from "./HeaderWidget";
import Nav from "./Nav";

const Header = () => {
  return (
    <section>
      <HeaderWidget />
      <Nav />
    </section>
  );
};

export default Header;
